<%= form_with(model: voucher_type, local: true) do |form| %>
  <% if voucher_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(voucher_type.errors.count, "error") %> prohibited this voucher_type from being saved:</h2>

      <ul>
      <% voucher_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :voucher_type_name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
