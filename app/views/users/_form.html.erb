<%= form_with(model: user, local: true) do |form| %>
  <% if user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(user.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
      <% user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :bd %>
    <%= form.number_field :bd, id: :user_bd %>
  </div>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :user_name %>
  </div>

  <div class="field">
    <%= form.label :rank_id %>
    <%= form.number_field :rank_id, id: :user_rank_id %>
  </div>

  <div class="field">
    <%= form.label :catering_id %>
    <%= form.number_field :catering_id, id: :user_catering_id %>
  </div>



  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
